# Allow update_engine to access thermal zone interfaces during OTA
allow update_engine sysfs_thermal:dir { search read getattr };
allow update_engine sysfs_thermal:file { read write getattr setattr open };
allow update_engine sysfs_thermal:lnk_file { read getattr };
